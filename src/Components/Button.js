import React from 'react'

const Button = ({data}) => {
  return (
    <div>
       <h3 className=' text-base text-center shadow-md p-2 rounded-lg mx-2'>{data}</h3>
    </div>
  )
}

export default Button
